import { Typography } from 'antd';

const { Title } = Typography;

export const AgentsPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Agents</Title>
      <p>Agent management will be displayed here.</p>
    </div>
  );
};
