import { Typography } from 'antd';

const { Title } = Typography;

export const PluginsPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Plugins</Title>
      <p>Plugin management will be displayed here.</p>
    </div>
  );
};
