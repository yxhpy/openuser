import { Typography } from 'antd';

const { Title } = Typography;

export const SchedulerPage: React.FC = () => {
  return (
    <div>
      <Title level={2}>Task Scheduler</Title>
      <p>Task scheduler will be displayed here.</p>
    </div>
  );
};
