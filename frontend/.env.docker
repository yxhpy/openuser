# Docker environment variables for frontend
# API requests will be proxied through nginx to the backend
# Empty base URL so API calls use full paths like /api/v1/auth/register
VITE_API_BASE_URL=
VITE_WS_URL=ws://localhost:3000/ws
